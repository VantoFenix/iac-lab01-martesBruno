version: '3.8'

services:
  # Configuración para la primera web (Puerto 4000)
  web01:
    build: ./src/web01
    container_name: servidor-bruno-4000
    ports:
       - "4000:80"

  # Configuración para la segunda web (Puerto 4001)
  web02:
    build: ./src/web02
    container_name: servidor-bruno-4001
    ports:
       - "4001:80"